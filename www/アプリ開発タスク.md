# アプリ開発タスク

## 開発ルール
- **GitHub push はこまめに実行する**（指示完了・ファイル更新の都度即push）
- ユーザーから来た指示を日付+番号で管理し、完了後も履歴として残す
- 新しい指示が来たら即番号付きで追加
- ステータス：🔄 未着手 / 🔧 作業中 / ✅ 完了 / ⏸️ 保留
- コミットIDも併記して追跡可能に

---

## 2026-04-30

### #1 スプラッシュ画面の ★NEW★ 表記を通常表示に戻す
- ステータス: ✅ 完了（コミット 83a6985）

### #2 ライダー名にひらがな+数字を許可
- ステータス: ✅ 完了（コミット 83a6985）
- 入力規則: ひらがな・半角/全角数字を許可（regex `[^぀-ゟー0-9０-９]`）

### #3 ライダー名の重複チェック
- ステータス: ✅ 完了（コミット 83a6985）
- NAS `/api/users` で他ユーザーと重複時はalertで拒否

### #4 数字を音声合成で正しく発音
- ステータス: ✅ 完了（コミット 83a6985）
- `_voiceName()` で表示は `てつ1`、音声は `てついち` に変換
- マップ: 0→ぜろ, 1→いち, 2→に, 3→さん, 4→よん, 5→ご, 6→ろく, 7→なな, 8→はち, 9→きゅう

### #5 SSTR/にっぽん応援カードをユーザー固有端末のみ表示
- ステータス: ✅ 完了（コミット 83a6985）
- 条件: `_myName === 'てつにん'` かつ APK 環境のみ表示
- iPhone 等他端末では非表示

### #6 カスタムプラン作成後、ルート検索音声＋行先音声ファイル生成
- ステータス: ✅ 完了
- 保存完了後に「ルートを検索して作成するのだ」音声＋ずんだもん表示
- 進捗オーバーレイ（行先音声 N/M、ルート計算 N/M、進捗バー）
- 行先音声: 各ストップ向けに `dest_<hash>` キーで NAS 事前生成
- ルート計算: 各セグメントを `_nasRoute` で並行計算（waypoint間ペア）
- 全完了で `startApp('custom')` で直接ナビ画面へ遷移（reload廃止）

### #7 全国コンビニ地図表示・営業中のみフィルタ
- ステータス: ✅ 完了
- 地図中心から半径2km以内のコンビニを Google Places nearbySearch で取得
- `opening_hours.open_now === false` の店舗は除外（営業中・情報なしのみ表示）
- チェーン別カラーマーカー（7-Eleven=橙, Family Mart=青, Lawson=水色, ミニストップ=黄, デイリー=赤, セイコーマート=橙）
- 常時表示・地図移動 1km 以上で再取得

### #8 地図のtilt(角度)を50°に設定
- ステータス: ✅ 完了
- ストリートモード(zoomMode=0)で `setTilt(50)` に変更（67.5°から）
- 後続: tilt 60° に再調整（2026-04-30）

### #9 自分位置を画面下方向にpanBy(0,-600)
- ステータス: ✅ 完了
- 🔍ボタン押下時に `map.panBy(0,-600)` で自分が表示上 600px 下方向に
- updateMapにsetTilt(60)復元してtilt維持

### #10 時間表示の先頭ゼロを削除
- ステータス: ✅ 完了
- `_fmtTime`: "07:00" → "7:00" に表示変換（24時超は「翌H:MM」）
- プラン作成リスト・マップポップアップ・残ストップ表示で _fmtTime を適用

### #11 夜間/日中切り替えが効かない件の修正
- ステータス: ✅ 完了（コード側）
- toggleNight で map 再生成時に `_gasIdleListener`/`_cvsIdleListener` の旧参照が残り、新mapに再登録されていなかった
- 再生成前に listener 参照と状態フラグを全クリアして、新map で initMap が再初期化するように修正
- 旧mapのDOMも `innerHTML=''` で完全クリア
- ⚠️ Cloud Console の Map Style 設定（MAP_ID_NIGHT に Dark スタイルを Associate＆Publish）が前提

### #15 SSTR2026 道の駅XLSXからスポット更新＋当日自動全表示
- ステータス: ✅ 完了
- `www/SSTR2026_道の駅一覧.xlsx` (69件) を `sstr2026_spots.json` に変換
  - 指定道の駅 47 / 能登復興応援ポイント 18 / 協力道の駅(2pt) 4
- `cDay==='day1'||cDay==='day2'` の時は `_sstr2026Show` トグル状態に関わらず強制表示
- `drawSSTR2026Markers` / `updateMap` / JSON load コールバックに day判定を追加

### #14 Android WebView で BGM(Amazon Music等)が止まる問題
- ステータス: 🔄 未着手・要決定
- 原因: WebView の HTML5 Audio が `AUDIOFOCUS_GAIN` を要求して他アプリを停止
- 対処案: ネイティブ Capacitor プラグインで `AUDIOFOCUS_GAIN_TRANSIENT_MAY_DUCK` + `USAGE_ASSISTANCE_NAVIGATION_GUIDANCE` を JS から制御
- 工数: 1-2時間（プラグイン作成・統合）

### #13 開発ログをWiFi接続時にNASへ蓄積アップロード
- ステータス: 🔧 クライアント側完了・NAS側エンドポイント追加が必要
- localStorage バッファ → 5分ごと/起動時/フォアグラウンド復帰時に NAS に同期
- 失敗時はバッファ保持（次回再試行）
- ロギング対象:
  - GPS (5秒間隔) lat/lng/speed/heading/accuracy
  - JS エラー (window.onerror, unhandledrejection)
  - ナビ操作 (zoomMode/nightMode/reroute)
  - 今後追加予定: stop_arrival, session_start/end, api_fail, 各種パフォーマンス指標
- NAS側に以下エンドポイント追加要：
  - POST /api/log/upload   ← 蓄積バッチ受信 → JSONLines追記
  - GET  /api/log/sessions ← 日別ログ一覧
  - GET  /api/log/sessions/<date> ← 詳細
- 実装テンプレート：`nas-api/log_endpoint_template.py`

### #12 SSTR/にっぽん応援スポットを CSV から年1回更新
- ステータス: ✅ 完了（コード側）
- CSVファイル：`www/data/plans.csv`（テンプレート空ファイル作成済み）
- カラム：`plan,order,time,name,lat,lng,pt,type,memo,dur,startNoHwy,noHwy,hasGoalTime`
- アプリ起動時に `_fetchPlansCsv()` 実行
  - localStorage `_plansCsvYear` と今年(YYYY)を比較
  - 異なる年 or 未取得なら fetch 実行
  - パース後 `STOPS[plan]` を上書き
- ユーザーが CSV を編集→push すれば、翌年のアプリ起動時に自動反映（年初に1回）

